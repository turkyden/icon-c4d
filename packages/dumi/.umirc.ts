import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'IconC4D',
  favicon: 'https://cdn.jsdelivr.net/gh/turkyden/icon-c4d/logo.svg',
  logo: 'https://cdn.jsdelivr.net/gh/turkyden/icon-c4d/logo.svg',
  outputPath: 'docs-dist',
  theme: {
    '@c-primary': '#ff6a00',
  },
  // more config: https://d.umijs.org/config
});
